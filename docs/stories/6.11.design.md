# Story 6.11 — Design: Wire Up Tournament Analytics

**Status: implemented and verified. See §7 for implementation notes and the verification table.**

---

## 1. The defect, restated

Three pieces of code were written to work together and none of them were connected.

| Piece | Evidence | State |
|---|---|---|
| `frontend/Rally/src/components/TournamentAnalyticsScreen.tsx` | `grep -rn TournamentAnalyticsScreen src App.tsx` → **0 hits**; absent from `components/index.ts`; in no navigator | orphaned UI |
| `backend/src/routes/tournament-analytics.ts` | `grep -rn tournament-analytics routes/index.ts server.ts` → **0 hits** | never mounted |
| `TournamentAnalyticsService` | `calculateParticipationMetrics` + `calculateBracketEfficiency` + `trackPlayerRankingChanges` | returns all 8 `AnalyticsData` fields |
| what the screen calls | `tournamentApi.getTournamentStats` → `/tournaments/:id/stats` | 6 fields, **2 of 8** overlap |

So `6.10.design.md` §3's "shape mismatch" is a symptom: the screen was written against an
`/analytics` contract that was never wired up, and the endpoint that satisfies it has been
unreachable since the day it was written.

---

## 2. Field-by-field match (the reason this is the right endpoint)

Screen's `AnalyticsData` vs the merged service output:

| `AnalyticsData` field | Source | Present |
|---|---|---|
| `totalRegistered` | `calculateParticipationMetrics` | ✅ |
| `participationRate` | `calculateParticipationMetrics` | ✅ |
| `completionRate` | `calculateParticipationMetrics` | ✅ |
| `matchesCompleted` | `calculateParticipationMetrics` | ✅ |
| `totalMatches` | `calculateBracketEfficiency` | ✅ |
| `bracketEfficiency` | `calculateBracketEfficiency` | ✅ |
| `averageUpsets` | `calculateBracketEfficiency` | ✅ |
| `rankingChanges` | `trackPlayerRankingChanges` | ✅ |

**8 of 8.** The service also returns `noShowRate` and `completedMatches`, which are harmless
supersets.

---

## 3. A second, latent bug found while designing the access model

The original route guarded with `authenticateToken` + `requireRole(['ORGANIZER','ADMIN'])` and then
checked ownership as:

```ts
{ organizer: (req as AuthRequest).user?.name || 'Unknown' }
```

**`req.user.name` does not exist.** Both `authenticateToken` and `optionalAuth` select only
`{ id, email, role }` from `prisma.user`, and the middleware's own `AuthRequest` interface declares
`user?: { id; email; role }` — no `name`.

So the predicate always evaluated to `{ organizer: 'Unknown' }` and **could never match**. Two
consequences:

1. The "organizer reads their own private tournament" path was **dead** — the route only ever
   served PUBLIC tournaments.
2. It type-checked anyway, because the route file declares its **own** local `AuthRequest` with
   `user?: { id; name?; email?; role }` — a shape that is never populated. **A type that describes a
   world the middleware does not create.**

### The correct authorization already exists

`prisma/schema.prisma` says so explicitly, next to the columns:

> `organizer` above is free text and **MUST NOT be used for authorization** (design §0 finding 13).
> These nullable columns carry the resolvable identity that `requireTournamentOrganizer` matches
> against: a verified JWT `userId` first, else a `deviceId`.

So the fix uses **`organizerUserId` / `organizerDeviceId`** with `resolveIdentity(req)` — Story 6.7's
machinery — not the free-text `organizer` string.

`requireTournamentOrganizer` itself is not reusable here: it **fails closed on anonymous callers**
(403), which is right for a mutation but wrong for a read of a PUBLIC tournament.

---

## 4. Design

### 4.1 Backend — `GET /api/v1/tournaments/:id/analytics`

```
optionalAuth  →  handler
```

- **`optionalAuth`** (Story 6.1), not `authenticateToken` + `requireRole`: the app is device-first,
  and a read of a PUBLIC tournament should not require a JWT. An invalid token still 401s rather
  than silently downgrading.
- Handler logic:
  1. Load `{ visibility, organizerUserId, organizerDeviceId }` for the id.
  2. Not found → `404` envelope, code `TOURNAMENT_NOT_FOUND`.
  3. `visibility === 'PUBLIC'` → allow.
  4. Otherwise compute the identity via `resolveIdentity(req)` and allow **only** the organizer:
     JWT → `identity.userId === organizerUserId`; device → `identity.deviceId === organizerDeviceId`;
     anonymous → deny.
  5. Denied → `404` (not 403), matching the original's "not found **or** access denied" so a private
     tournament's existence is not disclosed.
- **Standard envelope** on every path. The route currently returns a raw body
  (`res.json(analytics)`) and `{ error: '...' }` on failure — inconsistent with every sibling route,
  and unreadable by the frontend's error handling, which expects `error.code`.
- The `'Unknown'` sentinel is gone entirely.

### 4.2 Mounting

`routes/index.ts` gains an import and mounts it at `/tournaments`, **before** `tournamentRoutes`:

```ts
router.use('/tournaments', tournamentAnalyticsRoutes);
router.use('/tournaments', tournamentRoutes);
```

Verified no path collision: `tournaments.ts` defines no `/analytics/*`, no `/:id/analytics` and no
`/:id/feedback` (full route list checked). The router's other two routes become live as a
consequence — `/tournaments/analytics/trends` and `/tournaments/:id/feedback` — which is flagged in
§6 rather than silently ignored, since making a state-changing route live is not free.

### 4.3 Frontend

- `tournamentApi.getTournamentAnalytics(id)` reuses **`bracketRequest`**, which attaches
  `x-device-id`. This is the point: a logged-out, device-based organizer must be able to read their
  own private tournament, which is exactly why `bracketRequest` exists (Story 6.7 T05).
- `TournamentAnalyticsScreen`:
  - calls the new method instead of `getTournamentStats`;
  - `AnalyticsData` becomes the response type — no `as any`, no envelope read, no `/stats`.
- Reachability: register a `TournamentAnalytics` screen in `TournamentsStackNavigator`
  (`{ tournamentId, tournamentName }` params, matching what the screen already reads via
  `useRoute()`), plus an entry point from `TournamentDetailScreen`.

### 4.4 What is deliberately not changed

- `TournamentAnalyticsService` computation — it is correct and already has its own suite
  (`services/__tests__/tournamentAnalyticsService.test.ts`). This story is wiring, not maths.
- The *other*, mounted endpoint `GET /analytics/tournament/:tournamentId` (`routes/analytics.ts:173`)
  which reads the 4-column `tournament_analytics` table. Different data source, different
  granularity; left alone and its divergence recorded.
- `TournamentStats` type drift — separate defect.

---

## 5. Verification plan

1. Backend `./node_modules/.bin/tsc --noEmit` clean; backend suite green with counts.
2. New route tests: mounted/reachable; envelope on success; `404` for unknown id; PUBLIC readable
   anonymously; PRIVATE denied for anonymous; PRIVATE allowed for the organizer by **userId** and by
   **deviceId**; `401` on a malformed token.
3. **Live probe** — real server + real Postgres: create a tournament, hit the route, assert status,
   envelope, and that all 8 field names are present and non-`undefined`. Route work is not proven by
   mocks (SOP).
4. Frontend `npx tsc --noEmit` clean; frontend failure count not worse than baseline
   (9 suites / 19 tests, recorded in 6.10).
5. Clean-checkout verification.
6. Adversarial QA pass on the access model — specifically: can a non-organizer reach a PRIVATE
   tournament's analytics by supplying someone else's `x-device-id` alongside a valid JWT?

---

## 6. Known consequences / follow-ups

- **`/tournaments/analytics/trends` and `POST /tournaments/:id/feedback` go live** as a side effect
  of mounting the router. Both were written and guarded but never reachable. Feedback is
  state-changing and has no route-level tests; smoke coverage is added, and fuller coverage is
  flagged.
- `routes/tournament-analytics.ts` declares a local `AuthRequest` whose `user.name` field is never
  populated (see §3). After this change the route no longer reads `name`; the misleading local
  interface is removed if nothing else in the file needs it.

---

## 7. Implementation notes / deviations

### 7.1 What shipped

| # | Item | File |
|---|---|---|
| FIX-1 | `optionalAuth` replaces `authenticateToken` + `requireRole`; ownership decided by `resolveIdentity` against `organizerUserId` / `organizerDeviceId` | `backend/src/routes/tournament-analytics.ts` |
| FIX-2 | Standard envelope on success, 404, and 500 | same |
| FIX-3 | `'Unknown'` sentinel removed; local `AuthRequest` corrected to `{ id, email, role }` (`name` dropped — nothing populates it) | same |
| FIX-4 | `GET /analytics/trends` ownership clause changed from `organizer: req.user.name \|\| ''` to a conditional `organizerUserId` clause | same |
| DEV-1 | Router mounted at `/tournaments`, **before** `tournamentRoutes` | `backend/src/routes/index.ts` |
| DEV-2 | `TournamentAnalytics` + `TournamentRankingChange` types; `getTournamentAnalytics()` via `bracketRequest` | `frontend/Rally/src/services/tournamentApi.ts` |
| DEV-3 | Screen calls the new method; `AnalyticsData` becomes `TournamentAnalytics`; `as any` and `/stats` gone | `frontend/Rally/src/components/TournamentAnalyticsScreen.tsx` |
| DEV-4 | `TournamentAnalytics` screen registered; `TournamentsStackParamList` exported and shared | `frontend/Rally/src/navigation/MainTabNavigator.tsx`, `frontend/Rally/src/screens/TournamentListScreen.tsx` |
| DEV-5 | "View Analytics" entry point | `frontend/Rally/src/screens/TournamentDetailScreen.tsx` |

### 7.2 Deviations from §4

- **§4.3 said "register in `TournamentsStackNavigator`".** There is no separate
  `TournamentsStackNavigator` file — the tournament stack is declared inline in
  `MainTabNavigator.tsx`. Registered there. The param list type was *not* exported before,
  which is why `TournamentDetailScreen` had been reaching for `as never` casts; it is now
  exported as `TournamentsStackParamList` and `TournamentListScreen`'s private duplicate
  was deleted in favour of it.
- **§4.3 listed params as `{ tournamentId, tournamentName }`.** Kept, matching what the
  screen already reads through `useRoute()`.
- **§6 predicted "`/tournaments/:id/feedback` and `/analytics/trends` go live".** Confirmed
  live by the probe. `/analytics/trends` is read-only and was fixed as FIX-4. The feedback
  route is state-changing and is **still untested at the route level** — recorded as a
  follow-up in §6, not silently accepted.
- **FIX-4 was not in §4 at all.** It surfaced only because correcting the local
  `AuthRequest` interface made `req.user.name` a *type error* at
  `tournament-analytics.ts(122,53)`. The bug was already there; the type correction is what
  made the compiler say so. Worth noting as a general lesson: the misleading interface was
  actively suppressing a real defect.

### 7.3 Two things this story did *not* do

- **`TournamentRankingChange.finalRank` is `number | null`,** not `number` as the old
  screen-local type claimed. It is backed by `TournamentPlayer.finalRank`, which is unset
  until the tournament finishes. The UI renders it as text, so a null shows blank rather
  than crashing — but the old type was a third small lie in the same area.
- **The `/stats` payload mismatch for other callers is untouched.** `TournamentStats` still
  declares `totalGames` / `totalSets` / `currentRound` / `tournamentProgress` that the
  backend never sends. Out of scope (§Out of scope), still open.

### 7.4 Verification actually run

| Check | Result |
|---|---|
| Backend `tsc --noEmit` | exit 0 |
| Backend suite | **98 suites / 1146 passed / 3 skipped / 0 failed** (baseline 97 / 1135 — +1 suite, +11 tests, no new failures) |
| New route tests `routes/__tests__/tournament-analytics.test.ts` | **11/11** — mounted; envelope; 404 unknown; PUBLIC anonymous; PRIVATE anonymous → 404; PRIVATE organizer by userId; PRIVATE organizer by deviceId; **non-owner JWT + organizer's `x-device-id` → 404** (the §5.6 escalation case); malformed token → 401 |
| Frontend `tsc --noEmit` | exit 0 |
| Frontend suite | **9 failed suites / 19 failed tests / 118 passed** (baseline 9 / 19 / 115 — +3 passed, identical failing set) |
| `tournamentApi.bracket.test.ts` | **13/13** (10 pre-existing + 3 new analytics) |
| Live probe, real server + real Postgres | **11/11 PASS** |
| Causality | removing the `router.use` line → **9 failures and `text/html`** (SPA catch-all swallowing the request); restoring → 11/11 |
| Clean checkout (`git archive HEAD`) | identical to the working tree |

The causality result is the one worth keeping: before mounting, the route did not 404 — it
returned the **SPA's `index.html` with a 200**. A frontend calling it would have parsed HTML
as JSON. That is the failure mode this story removes, and it is invisible to any test that
mocks Express.

# Story 6.9 — Design: Mobile Release and Push Notifications

**Status: Phase-1 recon complete, identity decision RESOLVED, T3/T4/T5 specified. F1/T6/T7/T8 done and pushed. No Phase-2 work started.**

This document is the recon + design deliverable for Story 6.9. The headline has not changed: 6.9 is
**not** "finish the wiring". The push pipeline was **broken at the database level** (F1, now fixed),
and roughly half the story's acceptance criteria are **not verifiable from a development machine at
all**. What *has* changed since the first draft: F1 is repaired and committed (`8b645d6`), the
identity model is decided (Option C, §4), and — because the F1 fix was made — a **second, deeper
integration break surfaced that the F1 fix did not address and arguably exposed** (F9, §1). That
break, not F1, is now the thing standing between this story and AC 2.

Everything below is grounded in `file:line` or a probe I actually ran (§8). Where I could not verify
something, §9 says so plainly.

---

## 1. Headline findings

### F1 — ✅ FIXED (`8b645d6`): push token registration was impossible

Originally, `POST /notifications/register` wrote `playerId: deviceId`, but `PushToken.playerId` was a
**required FK to `MvpPlayer.id`**. Every call violated `push_tokens_playerId_fkey` (P2003) and the
route answered 500. A client could never register a token.

This is now fixed via **Option C** (§4):

- `PushToken.playerId` is **nullable and legacy** (`schema.prisma:1703`); no code path writes it.
- `PushToken.userId String?` was added with a FK to `User` and `onDelete: SetNull`
  (`schema.prisma:1713,1727`); `@@index([userId])` at `schema.prisma:1733`.
- `PushToken.deviceId` is unchanged (`schema.prisma:1708`).
- Migration `backend/prisma/migrations/20260917000000_story_6_9_push_token_identity/migration.sql`
  adds the column, the FK, the index, repoints `playerId`'s FK to `ON DELETE SET NULL`, and adds the
  CHECK `push_tokens_identity_present_check` (`migration.sql:44-46`).
- The route applies `optionalAuth` + `resolveIdentity(req)` and binds `userId`/`deviceId`, never
  `playerId` (`routes/notifications.ts:66-109`). Neither identity → **400 `VALIDATION_ERROR`**
  (`routes/notifications.ts:67-76`). The `deviceId` validator was relaxed to optional
  (`routes/notifications.ts:15`).
- The `findFirst({ where: { deviceId } })` null trap was fixed: an account-only caller looks up by
  `userId`, never a null `deviceId` (`routes/notifications.ts:81-83`).
- `routes/__tests__/notifications.test.ts` was rewritten to use the **real Postgres** (14/14 green).

**F1 is closed. Do not re-plan it.** See F9 for the consequence it did *not* resolve.

### F2 — there is no real push transport; it is a stub

```ts
// services/notificationService.ts:85-101  (sendToDevice — now redacts the token, still a stub)
const masked = raw.length > 4 ? `${raw.slice(0, 4)}…(${raw.length} chars)` : '(empty)';
console.log(`Sending push notification to ${token?.platform ?? 'unknown'} device:`, { token: masked, ... });
// Simulate successful delivery
return Promise.resolve();
```

It logs and resolves. AC 2 says delivery is wired "using `expo-server-sdk`" —
**`expo-server-sdk@^3.15.0` is installed (`backend/package.json:39`) and imported nowhere in
`backend/src`** (verified by grep, §8). It is a dead dependency. **This is the F2 state today; T3 is
still required** — but T3 must be built on the *live* send path, not this stub (see F3/F9/§5).

### F3 — there are THREE push implementations, and the "rich" one is not merely orphaned, it is schema-incompatible

The first draft framed this as two implementations. Recon for this pass found **three**, plus a
frontend orphan (F10):

| # | Module | Reachable? | Data access | State |
|---|---|---|---|---|
| 1 | `routes/notifications.ts` | **Yes** — mounted (`routes/index.ts:76`) | `prisma.pushToken` (client) | Correct + tested post-F1. Does **not** send. |
| 2 | `services/notificationService.ts` (599 lines, ~22 methods) | **No** — imported only by its own test (§8) | `prisma.$queryRaw` (raw SQL) | **Broken against the live schema** (below). |
| 3 | `utils/notificationHelper.ts` | **Yes** — 7 importers (§8) | `prisma.pushToken` (client) | Live send path; **mis-keyed** (F9); logs token prefixes (F8). |

The critical new fact about #2: **its raw SQL references columns that do not exist.** The service
queries `push_tokens` by `player_id`, `is_active`, `device_id`, `last_used` (`notificationService.ts:57,71-73,110-135`),
`notification_preferences` by `player_id`, `match_results`, `friend_requests`, … (`:166,225-244`) and
`notifications` by `player_id`, `is_read`, `read_at`, `delivered_at`, `sent_at` (`:26-40,281-293,321-341`).
But the live tables use **quoted camelCase** columns — verified directly against
`information_schema` (§8): `push_tokens` has `playerId/deviceId/isActive/lastUsed`, **no**
`player_id/is_active`; `notifications` has `playerId/isRead/readAt/deliveredAt/sentAt`, **no**
`player_id/is_read`; `notification_preferences` likewise. The migration file itself proves it:
`ALTER TABLE "public"."push_tokens" ALTER COLUMN "playerId" DROP NOT NULL;` (`migration.sql:28`).

So **every DB-touching method in the service would throw `column "player_id" does not exist` at
runtime.** It has never been executed against this schema. Its own test mocks `prisma.$queryRaw`
entirely (`services/__tests__/notificationService.test.ts:2-6`), which is why the incompatibility is
invisible to the suite — the same "a mocked persistence layer cannot pin a persistence contract"
lesson as F7.

**Therefore the premise of the original T5 ("the service is richer but unreachable") is false.** The
service is not richer; it is *broken*. There is no valuable raw-SQL token storage to preserve — it
cannot run. The 10 `NotificationHelpers` senders (`:444-603`) are thin wrappers over
`sendNotification`, which is itself broken → they are dead weight too (unreachable **and**
non-functional). See §5 for the decision this forces.

### F4 — AC 3 is unimplemented, not merely untested

`notificationPreferences` is read by **no production code**. The only references are inside the
orphaned service (`notificationService.ts:11,163,222,386,414`) and its test. There is no opt-out path
to honour. Still true. T4 required.

### F5 — the frontend push client is genuinely wired

Unlike the backend service, this side is reachable and used:

| File | Imported by |
|---|---|
| `src/services/NotificationService.ts` | 5 files, incl. `App.tsx`, `AppNavigator.tsx`, `useNotificationManager.ts` |
| `src/hooks/useNotificationManager.ts` | `SessionDetailScreen.tsx` |

`NotificationService.registerPushToken` POSTs `{ pushToken, deviceId, platform }` to
`${API_BASE_URL}/notifications/register` (`NotificationService.ts:227-235`) — which matches the
backend route exactly. So the frontend can produce a token and the backend can now accept one.

`expo-notifications ~0.29.14` and `expo-application ~6.0.2` are installed. `expo-device` is
**absent**. Token acquisition is additionally gated on a valid `EXPO_PUBLIC_PROJECT_ID`
(`NotificationService.ts:60-73`) — with none configured, no token is produced. That is a Phase-2
configuration concern, noted not solved.

### F6 — ✅ corrected by T8: AC 6 named a private method as public API

AC 6 listed `sendPushNotification` among the integration points; it is `private static`
(`notificationService.ts:52`). T8 corrected the story text. (Note: with F3/§5 the whole
`NotificationService` class is being retired anyway, so AC 6's "integrate with the service's three
named methods" should be re-read as "integrate with the consolidated send path".)

### F7 — ✅ addressed by T7: why the old test never caught F1

`routes/__tests__/notifications.test.ts` used to mock Prisma, so the FK was never enforced. It is now
a real-Postgres test (14/14 green) that *would* have failed on F1.

### F8 — token redaction is only partially done

T6 redacted the **service's** stub log (`notificationService.ts:85-101`). But the **live** helper
still logs token prefixes in three places:

```ts
// utils/notificationHelper.ts:58
console.log(`  → Would push to token ${token.token.substring(0, 12)}...`);
// :91  console.log(`  → Push to token ${token.token.substring(0, 12)}... : ${payload.title}`);
// :123 console.log(`  → Push to ${player.name}: ${payload.title}`);   // no token, OK
```

`:58` and `:91` leak the first 12 characters of a live push token. **AC 16 is therefore still
violated on the live path.** F8 is *not* closed; it moved. Completion is part of the T5 change list
(§6).

### F9 — 🆕 BLOCKER for AC 2: registration and delivery are keyed on **different columns**

The F1 fix made registration write `userId`/`deviceId` and **stop writing `playerId`**. But the live
send path still reads by `playerId`:

```ts
// utils/notificationHelper.ts:46-50   (notifySessionSubscribers)
const tokens = await prisma.pushToken.findMany({ where: { playerId: player.id } });
// :83-85    (notifyDevice — parameter is `userId`, but it queries `playerId`)
const tokens = await prisma.pushToken.findMany({ where: { playerId: userId } });
// :116-118  (notifyPlayer) — same
```

Post-F1, `playerId` is always NULL. So **every token registered through the route is invisible to
every sender**: a device registers successfully (200) and then receives **nothing**. AC 2 ("push
end-to-end") is not met — the two halves of the pipeline are wired to different keys.

Worse, `notifyDevice(userId, …)` is called with a **`User.id`** from four live sites —
`scheduler.ts:243` (`reminder.userId`), `friends.ts:62` (`receiverId`) and `:119`
(`response.senderId`), `messaging.ts:113` (`recipientId`) — and queries it against `playerId`. That
is the *same identity-confusion defect class as Story 6.8's D5* (`TournamentFeedback.playerId` held a
`User.id`), except here it is a `WHERE` clause rather than an FK, so there is no P2003 — just silent
zero rows. **This is the defect the next implementation pass must fix**, and it is why T3 (transport)
cannot be done in isolation: a real transport sending to zero tokens changes nothing observable.

### F10 — 🆕 the frontend has a second, orphaned client that targets endpoints the backend does not expose

`src/services/notificationApi.ts` (class `NotificationApiService`, singleton `notificationApi`) calls:

| Its call | Backend reality |
|---|---|
| `POST /notifications/token` (`:56`) | route is `POST /notifications/register` — **mismatch** |
| `DELETE /notifications/token` (`:76`) | route is `DELETE /notifications/register/:deviceId` — **mismatch** |
| `GET /notifications/preferences` (`:96`) | **no such endpoint** |
| `PUT /notifications/preferences` (`:111`) | **no such endpoint** |
| `PUT /notifications/:id/read`, `/read-all`, `GET /unread-count` | **no such endpoints** |

And `notificationApi.ts` is **imported by nothing** (grep across `frontend/src` → only its own file).
The wired client is `NotificationService.ts`. The preferences UI (`SettingsScreen.tsx:48,206-255`)
writes to **local component state only** — no API call. So **notification preferences never leave the
device today**. This is the fourth orphan in Epic 6 and it is directly relevant to T4 (the preference
keys the UI already shows are `friendRequests`, `messages`, `sessionInvites`, `matchResults`,
`achievements` — see §6 T4 for how they map).

---

## 2. Acceptance criteria — reality, one by one

| AC | Requirement | Reality |
|---|---|---|
| 1 | EAS production builds + documented repeatable pipeline | **Not started.** No `eas.json`, no `docs/release-process.md`. Not verifiable here. |
| 2 | Push end-to-end via `expo-server-sdk` + `PushToken` + route | **Still not met.** F1 fixed, but F2 (no transport) **and F9 (registration ≠ delivery keying)** both stand. |
| 3 | `NotificationPreferences` respected; opt-out honoured | **Not implemented** (F4). T4. |
| 4 | App Store / Play Store approval | **Not started.** Not verifiable here. |
| 5 | Push targeting uses Story 6.1 identity (guest tokens where allowed) | **Half done.** `resolveIdentity` now in the register route (F1 fix). Guest allow-list policy is open (Q2). |
| 6 | Integrate with the notification service's named methods | **Not integrated** (F3); the named methods are being retired (§5). |
| 7 | Frontend `expo-notifications` aligns with backend tokens | **Frontend yes (F5), backend now accepts (F1)** — but delivery is dead (F9), so alignment is untestable end-to-end. |
| 8 | Release builds → production API + observability (6.3) | **Not started.** 6.3's pieces exist (`metricsRegistry`, `correlationId`); nothing wires release config. |
| 9 | Crash-free sessions > 99.5% | **Not measurable here.** Needs production telemetry. |
| 10 | Push opt-in > 60% | **Not measurable here.** Needs production analytics. |
| 11 | Delivery, preference, opt-out covered by tests | **Partial.** Route contract now real-DB (T7). Delivery/preference/opt-out tests cannot exist — the features do not (F2/F4/F9). T3/T4 add them. |
| 12 | Staged rollout + documented rollback | **Partial.** `docs/rollback-procedures.md` exists (320 lines). No staged-rollout mechanism. |
| 13 | Store policy compliance (privacy manifest, data-safety) | **Not started.** |
| 14 | Secrets in CI, not the repo | **Blocked** — `ci.yml` is gitignored (Q4). |
| 15 | Build reproducibility documented | **Not started.** |
| 16 | Notification payloads contain no sensitive data | **Still violated on the live path** (F8: `notificationHelper.ts:58,91`). |

**9 of 16 ACs are not deliverable or not verifiable from this environment** (1, 4, 9, 10, 12 in part,
13, 14 blocked, 15). That remains the single most important fact about this story's scope. After
T3/T4/T5 land, the *closable-here* set is **2, 3, 5, 6, 7, 11, 16** (and the addressable part of 8).

---

## 3. Proposed scope split

### Phase 1 — deliverable and verifiable here

| ID | Task | Status |
|---|---|---|
| T1 | Fix the `PushToken.playerId` contract (F1) | **✅ DONE** — Option C, `8b645d6`. |
| T2 | Wire identity via Story 6.1 (AC 5) | **Partial** — register route done (F1 fix); guest allow-list gated on Q2. |
| T3 | Implement the real transport (AC 2, F2) | **Specced in §6.** Must also resolve F9. |
| T4 | Honour `NotificationPreferences` (AC 3, F4) | **Specced in §6.** |
| T5 | Resolve the three-implementations problem (F3) | **Decided in §5; change list in §6.** |
| T6 | Stop logging tokens (AC 16, F8) | **Partial** — service redacted; live helper still leaks (F8). Folded into T5 list. |
| T7 | Replace the mock-based route test with a real-Postgres one (F7) | **✅ DONE.** |
| T8 | Correct the story text (F6) | **✅ DONE.** |

### Phase 2 — needs accounts, credentials, or production telemetry (not this environment)

- EAS build/submit profiles and a real store release (AC 1, 4, 13)
- Crash-free and opt-in measurement (AC 9, 10)
- Staged rollout mechanics (AC 12)
- Secrets provisioning in CI (AC 14) — depends on the `ci.yml` decision (Q4)
- `expo-device` install + `EXPO_PUBLIC_PROJECT_ID` provisioning so real device tokens are produced (F5)

Phase 2 should be tracked as its own workstream with an owner who has App Store Connect and Play
Console access. It cannot be "done" by an agent on this machine, and I would rather say so than
produce artefacts that look complete and are not. **See Q3 for the recommendation.**

---

## 4. The identity decision — ✅ RESOLVED (Option C)

`PushToken.playerId` → `MvpPlayer.id`, but `MvpPlayer` is **session-scoped** (`sessionId` required,
`schema.prisma:242`) and `MvpPlayer.userId` is NULL-by-design for guests (`schema.prisma:249`). So a
*player* is not a stable identity across sessions, while a push token is exactly that — a durable
device/account handle.

Of the three options in the prior draft (A key-to-device, B key-to-account, C both), **Option C was
chosen and committed** (`8b645d6`): a token is keyed to a **user and/or a device, at least one
required**, enforced by the DB CHECK `push_tokens_identity_present_check`. Rationale (from the
migration's own header, `migration.sql:8-12`): it mirrors Story 6.7's tournament-ownership pattern
(`organizerUserId`/`organizerDeviceId`) for the same reason — a device-first app where identity must
resolve from either a JWT or a device id — and it is a strict superset of "key to device", so it
serves guests *and* account holders as AC 5 requires.

**This closes open question #1 from the prior draft. No further identity decision is pending.** The
one remaining identity-adjacent open item is the *guest allow-list* (Q2), which is policy, not schema.

---

## 5. T5 — the decision: retire `services/notificationService.ts`, consolidate on the route + the live helper

The prior T5 asked which of two access paths to keep. Recon for this pass shows there are **three**
(F3) and that the framing was wrong: the "richer" service is **broken against the live schema**, so
"make the route delegate to the service" would import dead code into the live path. Decision:

> **Retire `services/notificationService.ts` entirely. Keep `routes/notifications.ts` (correct,
> tested, Option-C token storage). Make `utils/notificationHelper.ts` the single send path, re-keyed
> on `userId`/`deviceId` (F9) and given a real transport (T3) and preference gating (T4). Port only
> the three *pure* helpers from the service.**

### Justification

1. **The service cannot run, so it has nothing to offer as a whole.** Every DB query in it names a
   column that does not exist (F3, verified against `information_schema`). "Delegate to the service"
   is not a refactor; it is a rewrite, and the result would be a *worse* starting point than the route
   (which is already correct and tested).
2. **The route holds the one piece that was just fixed and verified.** Its Prisma-client, Option-C
   token storage passed 14 real-DB tests. Deleting it would throw that away and force its re-derivation
   from a raw-SQL implementation that does not work.
3. **The live fan-out point is `utils/notificationHelper.ts`, not the route.** The route *registers*
   tokens; it never *sends*. Seven modules already call the helper (§8): `scheduler.ts`,
   `mvpSessions.ts` (×2), `scoring.ts`, `pairings.ts`, `friends.ts`, `messaging.ts`,
   `socket/notificationHandlers.ts`. Putting the transport (T3) and gating (T4) **there** makes all
   seven gain real delivery in one change. Wiring the route would send nothing.
4. **Retiring the service is safe — nothing imports it.** Verified by grep across `backend/src` and
   `frontend`: the only importer is `services/__tests__/notificationService.test.ts` (§8). Removing
   both files cannot break any caller.

### What happens to each piece of the service

| Piece | Lines | Disposition |
|---|---|---|
| `sendToDevice` stub | `:85-101` | **Deleted.** Replaced by the real transport (T3). |
| Raw-SQL token storage (`registerPushToken`, `unregisterPushToken`) | `:106-158` | **Deleted, not migrated.** It writes to columns that do not exist; the route's Prisma-client storage is the survivor. |
| Preferences CRUD (`get/updateNotificationPreferences`) | `:163-267` | **Deleted.** Replaced by a small Prisma-client `notificationPreferences.ts` (T4). |
| Notifications CRUD (`getNotifications`, mark-read ×2, unread-count, cleanup) | `:272-381` | **Deleted.** No route exposes them and no caller exists — dead. |
| `isNotificationEnabled`, `isInQuietHours`, `parseTimeString` | `:386-440` | **Ported** (pure functions — the only genuinely reusable part). See T4. |
| 10 `NotificationHelpers` senders | `:444-603` | **Deleted.** Unreachable **and** non-functional (they call the broken `sendNotification`). Confirmed dead weight, not "rich". |
| `sendPushNotification` (`private`) | `:52-80` | **Deleted.** Its role moves to the helper + transport. |

### Consequence for T3 and T4 (where they live)

- **T3 lives in a new `backend/src/services/pushTransport.ts`**, consumed by
  `utils/notificationHelper.ts`. It does **not** live in the deleted service.
- **T4 lives in a new `backend/src/services/notificationPreferences.ts`** plus a small
  `NotificationPreferences.userId` schema addition and two route endpoints, consumed by
  `utils/notificationHelper.ts`. It does **not** live in the deleted service.
- **T5's deletion happens first** (step 1 of §6), so T3/T4 are never written against the broken file.

---

## 6. Implementation spec — T3, T4, T5

### T3 — real push transport (AC 2, F2)

**Goal:** replace the stub with a real `expo-server-sdk` send, testable with **no Expo credentials**,
that also unblocks delivery (with F9).

**New file:** `backend/src/services/pushTransport.ts`

**Seam for testing without credentials:** the SDK client is **constructor-injected**. Tests pass a
fake client (or a fake `PushTransport`); production constructs the real `Expo`. No token, no network,
no credentials in any test.

```ts
import { Expo } from 'expo-server-sdk';

export interface PushMessage {
  to: string;                          // Expo push token
  title: string;
  body: string;
  data?: Record<string, unknown>;
  sound?: 'default' | null;
  priority?: 'default' | 'normal' | 'high';
}

export type PushTicketError =
  | 'DeviceNotRegistered' | 'InvalidCredentials' | 'MessageTooBig'
  | 'MessageRateExceeded' | 'ProviderError' | 'DeveloperError' | 'ExpoError';

export interface PushTicket {
  token: string;                       // echoed for index-safe correlation
  status: 'ok' | 'error';
  error?: PushTicketError;
  message?: string;
}

export interface PushTransport {
  send(messages: PushMessage[]): Promise<PushTicket[]>;
}

/** Minimal shape we depend on, so tests can fake the SDK. */
export interface ExpoClientLike {
  chunkPushNotifications(m: any[]): any[][];
  sendPushNotificationsAsync(m: any[]): Promise<any[]>;
}

export class ExpoPushTransport implements PushTransport {
  constructor(private readonly client: ExpoClientLike = new Expo({
    accessToken: process.env.EXPO_ACCESS_TOKEN,   // undefined is fine for public Expo push
    useFcmV1: true,
  })) {}

  async send(messages: PushMessage[]): Promise<PushTicket[]> { /* chunk → send → map, see below */ }
}
```

**Behaviour the spec fixes** (verified against the installed `expo-server-sdk@3.15.0` typings,
`build/ExpoClient.d.ts`):

- **Payload construction.** Each `PushMessage` → `{ to, title, body, data, sound: 'default',
  priority: 'high' }`. `ExpoPushMessage.to` may be a single token; we send one message per token so
  tickets map 1:1 to tokens (`ExpoClient.d.ts:56-80`).
- **Batching.** Use `client.chunkPushNotifications(...)` (limit 100,
  `ExpoClientValues.d.ts:7`) and call `sendPushNotificationsAsync` once per chunk. Never send an
  unbounded array.
- **Index-safe correlation.** `sendPushNotificationsAsync` returns tickets aligned to the input
  array (`ExpoClient.d.ts:16-26`). Map each ticket back to its token by index and **echo the token**
  on the returned `PushTicket`, so callers never re-derive it.
- **Skip non-Expo tokens.** Before sending, filter with `Expo.isExpoPushToken(token)`
  (`ExpoClient.d.ts:14`); a token that fails is returned as a `DeveloperError` ticket and is
  **deactivated** (same path as `DeviceNotRegistered`).
- **Per-token error isolation.** One error ticket must not abort the batch; other tokens still send.
- **`DeviceNotRegistered` handling.** For any returned ticket with `status: 'error'` and
  `details.error === 'DeviceNotRegistered'` (`ExpoClient.d.ts:93-101`), **deactivate the token row**:
  `prisma.pushToken.updateMany({ where: { token }, data: { isActive: false } })`. This is the prune
  contract and it **requires a test** (below).
- **Transport failure.** If `sendPushNotificationsAsync` rejects (network/timeout), catch it and
  return error tickets for that chunk — never throw into the caller's fire-and-forget sites.
- **Redaction (F8).** Never log the full token; log at most the existing 4-char mask.

**Receipts (call out explicitly):** Expo delivers `DeviceNotRegistered` reliably via **receipts**
(`getPushNotificationReceiptsAsync`, `ExpoClient.d.ts:27-29`), not always in the immediate ticket.
Full receipt handling needs a scheduler job and receipt-id persistence. **Recommendation:** implement
the immediate-ticket prune in T3 (testable now), and file receipt polling as a **Phase-1.5** task —
it is testable with a fake client but is a scheduling/state addition beyond AC 2's "wired" bar.

**Consumer change (also fixes F9):** `utils/notificationHelper.ts` builds `PushMessage[]` from the
tokens it finds and calls `transport.send(...)`, replacing the `console.log` placeholders at `:58`
and `:91`.

**Tests** — `backend/src/services/__tests__/pushTransport.test.ts` (fake client, no credentials):

1. builds a correctly-shaped message per token (`to/title/body/data/sound/priority`).
2. batches >100 messages into the expected number of `sendPushNotificationsAsync` calls.
3. maps index-aligned tickets back to the correct tokens (echoes `token`).
4. a non-Expo token is returned as a `DeveloperError` ticket and is not sent.
5. one error ticket does not abort the rest of the batch.
6. `DeviceNotRegistered` ticket → the token row is deactivated (`isActive: false`) — real DB or
   mocked `prisma.pushToken.updateMany`, asserted explicitly.
7. `sendPushNotificationsAsync` rejection → error tickets, no thrown exception.
8. the full token never appears in any `console.*` call (spy on console).

### T4 — honour `NotificationPreferences` (AC 3, F4)

**Goal:** read preferences before send and suppress on opt-out / quiet hours, on the live path.

**Schema (dependency):** `NotificationPreferences` currently has only `playerId String? @unique` and
`deviceId String? @unique` (`schema.prisma:1668-1669`) — **no `userId`**. To honour preferences for
account users we must mirror Option C here:

- Add `userId String? @unique` + `user User? @relation(..., onDelete: SetNull)` + `@@index([userId])`
  to `NotificationPreferences`; mark `playerId` legacy (nullable, no longer written).
- New migration `backend/prisma/migrations/2026…_story_6_9_notification_preferences_identity/`.
- No data migration: the table is **empty** (0 rows, verified §8), so nothing is reinterpreted.

**New file:** `backend/src/services/notificationPreferences.ts`

```ts
export type PrefKey =
  | 'matchResults' | 'achievements' | 'friendRequests' | 'challenges'
  | 'tournamentUpdates' | 'socialMessages' | 'sessionReminders';

/** Gating table: live send-type string → preference key (null = always allowed). */
export function mapTypeToPreferenceKey(type: string): PrefKey | null;

export function isNotificationEnabled(type: string, prefs: NotificationPreferences): boolean;

export function isInQuietHours(prefs: NotificationPreferences, now?: Date): boolean;

/** Prisma-client read; user-first, then device; defaults when none. */
export async function getPreferencesFor(
  identity: { userId?: string | null; deviceId?: string | null },
): Promise<NotificationPreferences>;

export async function shouldSuppress(
  identity: { userId?: string | null; deviceId?: string | null },
  type: string,
  now?: Date,
): Promise<{ suppress: boolean; reason?: 'opted_out' | 'quiet_hours' }>;
```

**Gating table** (must cover the **live string types**, which are *not* the `NotificationType` enum —
see `scheduler.ts:119,246`, `scoring.ts:174`, `mvpSessions.ts:848`, `friends.ts:65,122`,
`messaging.ts:116`):

| Live send `type` | Preference key | Notes |
|---|---|---|
| `MATCH_RESULT`, `SCORE_RECORDED`, `MATCH_REMINDER` | `matchResults` | match mechanics |
| `ACHIEVEMENT_UNLOCK` | `achievements` | |
| `FRIEND_REQUEST`, `FRIEND_ACCEPTED` | `friendRequests` | |
| `CHALLENGE_RECEIVED`, `CHALLENGE_RESPONSE` | `challenges` | |
| `TOURNAMENT_UPDATE` | `tournamentUpdates` | |
| `SESSION_REMINDER` | `sessionReminders` | |
| `SOCIAL_MESSAGE`, `NEW_MESSAGE` | `socialMessages` | default OFF (`schema.prisma:1677`) |
| `PLAYER_JOINED`, `PAIRING_GENERATED`, `GAME_READY`, `NEXT_UP`, `SESSION_STARTING`, `SESSION_UPDATED` | `sessionReminders` | **session-activity; no dedicated key.** Recommended mapping; flag as an open sub-decision (§9). |
| `SYSTEM_ANNOUNCEMENT` | `null` (always) | matches the service's own rule (`notificationService.ts:404-405`) |
| unknown | `null` (always) | fail-open, logged once |

**Suppression semantics:**
- `pushEnabled === false` → suppress **all** pushes (`reason: 'opted_out'`).
- the mapped key is `false` → suppress that type (`reason: 'opted_out'`).
- inside quiet hours → suppress (`reason: 'quiet_hours'`).
- suppression **skips the push only**; it must not throw and must not block other recipients.
- **Device-only (guest) tokens have no preferences row** → treat as *defaults* (fail-open per the
  schema defaults), except where Q2's guest allow-list denies the type outright.

**Route surface (so preferences are actually reachable — F10):** add to `routes/notifications.ts`:

- `GET /notifications/preferences` → `getPreferencesFor(resolveIdentity(req))`.
- `PUT /notifications/preferences` → validate + upsert for the resolved identity.
  (`optionalAuth` + the same "at least one identity" 400 guard as `/register`.)

These are the endpoints `frontend/.../notificationApi.ts:96,111` already expects. Wiring
`SettingsScreen.tsx` to them (and retiring or aligning `notificationApi.ts`) is a **frontend
follow-up** — flagged, not specced here.

**Tests** — `backend/src/services/__tests__/notificationPreferences.test.ts`:

1. `mapTypeToPreferenceKey` table-driven over every live type above (+ unknown → `null`).
2. `isNotificationEnabled`: false when the key is false; `SYSTEM_ANNOUNCEMENT` always true.
3. `isInQuietHours`: inside / outside / overnight window (22:00–06:00) / no window configured.
4. `getPreferencesFor`: stored row for `userId`; defaults when none; prefers `userId` over `deviceId`.
5. `shouldSuppress`: `opted_out` (master off), `opted_out` (type key off), `quiet_hours`, and
   not-suppressed.
6. **Integration (real DB):** `PUT /preferences { pushEnabled:false }` for a user, then a send for
   that user is suppressed — assert `transport.send` receives **no** message for that token.

### T5 — the consolidation, as an ordered change list

Do these in order; steps 1–3 are prerequisites for T3/T4.

1. **Port before delete.** Move `isNotificationEnabled`, `isInQuietHours`, `parseTimeString`
   (`notificationService.ts:386-440`) into `notificationPreferences.ts` (T4). *(Note the original's
   misleading comment at `:426` labels the same-day branch "22:00 to 08:00"; the logic is correct,
   the label is not — carry the fix into the port.)*
2. **Add the `NotificationPreferences.userId` schema + migration** (T4 dependency).
3. **Delete** `backend/src/services/notificationService.ts` **and**
   `backend/src/services/__tests__/notificationService.test.ts`. Re-run `grep` to confirm zero
   remaining importers. *(This is where the raw-SQL token storage and the 10 dead senders go.)*
4. **Re-key the live send path (F9).** In `utils/notificationHelper.ts`:
   - `notifyDevice(userId, …)` → `where: { userId }` (not `playerId`).
   - `notifySessionSubscribers` → for each `MvpPlayer`, resolve tokens by its `userId` **and** its
     `deviceId` (guests have `userId` NULL by design, `schema.prisma:249`; accounts may have both).
     Query `where: { OR: [{ userId: player.userId }, { deviceId: player.deviceId }] }`, skipping null
     legs.
   - `notifyPlayer` → same re-key.
5. **Introduce `pushTransport.ts` (T3)** and replace the `console.log` placeholders at
   `notificationHelper.ts:58,91` with `transport.send(...)`. **This also completes F8** (no token
   prefix in logs).
6. **Add preference gating (T4)** into the helper: before enqueuing a message for an identity, call
   `shouldSuppress`; drop the message when suppressed.
7. **Add `GET`/`PUT /notifications/preferences`** to `routes/notifications.ts` (T4 surface).
8. **Frontend follow-up (flagged, not in T5):** retire or align `notificationApi.ts` (F10) and wire
   `SettingsScreen.tsx` to the new preferences endpoints.

**Definition of done for T5:** `grep` finds no reference to `notificationService`/`NotificationHelpers`;
`grep` finds no `playerId` token lookup in `utils/notificationHelper.ts`; `expo-server-sdk` is imported
by `pushTransport.ts`; and no `console.*` in the send path prints more than 4 chars of a token.

---

## 7. Open questions for the PO

**Q1 — ✅ RESOLVED.** Which identity model for push tokens? **Option C, committed in `8b645d6` (§4).**

**Q2 — guest notifications: recommend a concrete allow-list.** AC 5 says "guest tokens where allowed";
AC 16 forbids sensitive payloads. A device-only token has no account, so it should receive only
**session/event mechanics that contain no PII beyond a first name already visible to session
participants**. Recommendation:

- **ALLOW (guest tokens):** `SESSION_REMINDER`, `SESSION_STARTING`, `SESSION_UPDATED`,
  `PLAYER_JOINED`, `PAIRING_GENERATED`, `GAME_READY`, `NEXT_UP`, `SCORE_RECORDED`.
- **DENY (account-only):** `FRIEND_REQUEST`, `FRIEND_ACCEPTED`, `CHALLENGE_*`, `SOCIAL_MESSAGE` /
  `NEW_MESSAGE`, `ACHIEVEMENT_UNLOCK`, `MATCH_RESULT`, `TOURNAMENT_UPDATE` — these are account-scoped
  and can leak the social graph or account history.
- **`SYSTEM_ANNOUNCEMENT`:** deny to guest tokens by default (do not broadcast to unauthenticated
  devices) unless the PO explicitly wants guest-wide announcements.
- Enforce as a deny-by-default allow-list in the send path, so a new type is guest-safe only if
  someone opts it in.

**Q3 — Phase 2 scope: recommend YES, split it out.** Nine ACs (1, 4, 9, 10, 12-part, 13, 14, 15)
cannot be closed or measured on this machine. Keeping them inside 6.9 makes "Done" unreachable and
encourages exactly the "artefacts that look complete and are not" failure this recon has been careful
to avoid. Recommendation: **re-scope 6.9 to the Phase-1 ACs (2, 3, 5, 6, 7, 11, 16 + the addressable
part of 8) and create a separate release-ops workstream (e.g. 6.10) with an owner who has EAS,
App Store Connect and Play Console access** for the rest. A story that can reach "Done" is worth more
than one that cannot.

**Q4 — ✅ ANSWERED.** Does 6.9 wait for the `ci.yml` decision? **`ci.yml` is not merely untracked — it
is explicitly gitignored.** `.gitignore:20` contains `.github/workflows/ci.yml`; `git check-ignore -v`
confirms it and `git ls-files .github/workflows/` lists only `codeql.yml` and `release.yml`. So **no CI
runs on push at all**, and AC 14 (secrets in CI) has no workflow host. AC 14 is **blocked** until
someone un-ignores/tracks the workflow — a repo-hygiene decision, not a 6.9 code change.

---

## 8. Verification I actually ran for this pass

| Check | Method | Result |
|---|---|---|
| `routes/notifications.ts` mounted? | read `routes/index.ts` | **Yes** — `/notifications` (line 76) |
| `notificationService.ts` importers | grep across `backend/src` + `frontend` | **Only its own test** (`services/__tests__/notificationService.test.ts`) |
| `expo-server-sdk` used in code? | grep across `backend` | **No** — installed (`package.json:39`), imported nowhere |
| Service raw-SQL columns exist? | `information_schema.columns` probe on live `badminton_group` | **NO** — real cols are camelCase (`playerId/isActive/deviceId/lastUsed`); service queries `player_id/is_active/…` → would throw |
| `notificationHelper.ts` importers | grep | **7 live modules** (scheduler, mvpSessions ×2, scoring, pairings, friends, messaging, socket) |
| `notifyDevice` callers pass what id? | read `friends.ts:62,119`, `messaging.ts:113`, `scheduler.ts:243` | **`User.id`** — queried against `playerId` (F9) |
| `notificationApi.ts` importers | grep across `frontend/src` | **None** (F10) |
| `SettingsScreen` persists prefs? | read `SettingsScreen.tsx` | **No** — local state only (F10) |
| `ci.yml` tracked? | `git ls-files` / `git check-ignore -v` | **Gitignored** (`.gitignore:20`), untracked (Q4) |
| Row counts | `SELECT count(*)` on live DB | `push_tokens=0`, `notification_preferences=0`, `notifications=0` |
| `expo-server-sdk` API surface | read installed `build/ExpoClient.d.ts` (v3.15.0) | `sendPushNotificationsAsync`, `chunkPushNotifications`, `isExpoPushToken`, `DeviceNotRegistered` in `details.error` |
| `PushToken` FK semantics | read migration `…push_token_identity/migration.sql` | `userId` FK `ON DELETE SET NULL`; `playerId` repointed to `SET NULL`; CHECK requires ≥1 identity |

---

## 9. Assumptions and things I could NOT verify

- **I did not run the full backend suite** — tests share one live Postgres; I used read + focused
  probes only, per the task constraint.
- **`crypto.randomUUID()` in the orphaned service has no `crypto` import** (`:24,131,248`). On Node
  22 (`v22.22.2`) the global `crypto` exists so this *would* work — but the surrounding SQL does not,
  so it is moot. I mention it only as further evidence the file has never run end-to-end.
- **Session-activity types have no dedicated preference key.** I mapped `PLAYER_JOINED`,
  `PAIRING_GENERATED`, `GAME_READY`, `NEXT_UP` to `sessionReminders`. That is a judgement call, not a
  verified product decision — flag for PO/PM if session-activity should be independently mutable.
- **Receipt-based pruning is not implemented by T3.** I verified the SDK exposes
  `getPushNotificationReceiptsAsync`, but did not verify how often `DeviceNotRegistered` arrives only
  as a receipt vs. an immediate ticket. T3 prunes on the immediate ticket; receipt polling is
  recommended as Phase-1.5.
- **Guest-token delivery path is unverified end-to-end.** With `push_tokens` empty and no Expo
  credentials, I could not exercise a real send. T3/T4 tests use fakes by design; a live send needs
  Phase-2 credentials.
- **I did not inspect `MvpSession`/`MvpPlayer` back-fill semantics for `deviceId` uniqueness**; the
  `PushToken` `@@unique([deviceId, isActive])` (`schema.prisma:1731`) means only one active token per
  device id — worth confirming against how the frontend generates `deviceId` (out of scope here).
